<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.bharathksunil.interrupt.events.ui.fragments.CoordinatorsEventsInfoFragment">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="@string/tv_heading_coordinating_events"
        android:textAppearance="@style/Dashboard.TextAppearance.Title"
        android:textColor="@color/primary_dark" />

    <include layout="@layout/layout_progress_bar" />

    <TextView
        android:id="@+id/tv_empty_prompt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/tv_empty_coordinating_events"
        android:visibility="gone"
        tools:visibility="visible" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_events"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</LinearLayout>
